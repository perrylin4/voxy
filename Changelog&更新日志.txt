Neo Voxy 更新日志 / Changelog
（懒得写，这是ai生成的）
（Too lazy to write , this was generated by AI）

## 0.5.0-alpha.4（1.21.1 NeoForge）— 2026-09-22

### 简体中文

- 修复开启光影时 LOD 与原版区块亮度不一致的问题。

### English

- Fixed inconsistent brightness between LOD and vanilla chunks with shaders enabled.



## 0.4.12（1.21.1 NeoForge 联动版）— 2026-09-17

### 简体中文

- 扩展 LOD 区段坐标高度并保留普通世界旧缓存键，修复 Tellus 扩展高度世界中的坐标溢出崩溃。

### English

- Extended the LOD section-coordinate height range while preserving existing ordinary-world cache keys, fixing coordinate-overflow crashes in extended-height Tellus worlds.

## 0.4.11（1.21.1 NeoForge 联动版）— 2026-09-17

### 简体中文

- 清理失效源码与注释代码，精简源码注释，并将 README 拆分为原版功能和 Mod 联动表格。
- 将机械动力：航空学的载具渲染距离统一为区块滑块，默认跟随 LOD。
- 修复 ByePregen 自定义区块调色板导致的摄取报错与空白 LOD。
- 专属 LOD 网格、机械动力轨道与动态对象及 Sable 载具不再参与像素级 LOD 淡入。
- 修复机械动力列车接管空环、边界闪烁与水体遮挡异常。

### English

- Removed disabled source and commented-out code, simplified comments, and split the README into vanilla-feature and mod-integration tables.
- Changed the Create Aeronautics contraption render distance to the shared chunk slider, defaulting to Follow LOD.
- Fixed ingest errors and empty LODs caused by ByePregen custom chunk palettes.
- Excluded dedicated LOD meshes, Create tracks and moving objects, and Sable vehicles from per-pixel LOD fading.
- Fixed missing Create trains, boundary flicker, and water occlusion during handover.

## 0.4.10（1.21.1 NeoForge 联动版）— 2026-09-16

### 简体中文

- 修复纯 Copycats+ 多状态方块机械结构生成空远景网格，并持久化机械结构的伪装材质。
- 修复未填充 Copycats+ 方块在 LOD 中重复烘焙并显示错误材质。
- 修复未填充 Copycats+ 方块的 LOD 几何精度和楼梯材质回退。
- 完善可选联动的加载检测，并为专属模型加入仅限完整方块之间的保守面剔除。

### English

- Fixed empty distant meshes for contraptions made only from multi-state Copycats+ blocks and persisted their camouflage materials.
- Fixed repeated LOD baking and incorrect materials on unfilled Copycats+ blocks.
- Fixed inaccurate LOD geometry and stair material fallback for unfilled Copycats+ blocks.
- Tightened optional integration gates and added conservative face culling only between complete custom-model blocks.

## 0.4.9（1.21.1 NeoForge 联动版）— 2026-09-16

### 简体中文

- 新增 Create: Copycats+ 3.0.9 联动，持久化多部件材质并生成斜坡、切片、门、传动杆和齿轮的专属 LOD 网格。
- 视频设置新增“机械动力：伪装方块+”开关与距离选项。

### English

- Added Create: Copycats+ 3.0.9 integration with persistent per-part materials and dedicated LOD meshes for slopes, slices, doors, shafts, and cogwheels.
- Added Create: Copycats+ toggle and distance options to Video Settings.

## 0.4.8（1.21.1 NeoForge 联动版）— 2026-09-16

### 简体中文

- 将 Sable 兼容与构建基线更新至 2.0.5。
- 完善 Ecliptic Seasons 0.15.0-rc+ 联动，支持远景季节积雪、结冰水体、季节模型与颜色。
- 阻止同时加载不兼容的 Ecliptic Seasons: Voxy Compact。

### English

- Updated the Sable compatibility and build baseline to 2.0.5.
- Expanded Ecliptic Seasons 0.15.0-rc+ integration with distant seasonal snow, frozen water, seasonal models, and colours.
- Blocked the incompatible Ecliptic Seasons: Voxy Compact mod.

## 0.3.5（1.20.1 Forge 客户端版）— 2026-09-12

### 简体中文

- 修复半透明方块被错误标记为 Alpha-cutout，导致玻璃、染色玻璃及部分模组装饰方块在 LOD 烘焙时叠合为黑色线框或面框的问题。
- 规范化 Forge 模组模型省略的顶点 Alpha，并为藤蔓、花草、栏杆等 cutout 卡片保留双面烘焙，避免整面透明度被清空或背面缺失。

### English

- Fixed translucent blocks being treated as alpha-cutout cards, which could collapse glass, stained glass, and modded decorative faces into dark outlines or panels during LOD baking.
- Normalized omitted Forge baked-quad vertex alpha and preserved two-sided baking for cutout cards such as vines, plants, and bars, preventing erased face alpha and missing back faces.

## 0.3.4（1.20.1 Forge 客户端版）— 2026-09-12

### 简体中文

- 修复原版/均衡树叶模式仍将所有树叶强制按实心模型烘焙、关闭 Alpha 丢弃的问题；透明区带黑色 RGB 的模组树叶不再在 LOD 中显示为黑色线框或面框。
- 统一使用树叶标签与原版树叶类型识别渲染层，使使用自定义方块类但正确加入 `minecraft:leaves` 标签的模组树叶也进入正确的 cutout 路径。

### English

- Fixed all leaves being baked as forced-solid geometry even in original/balanced leaf modes, which disabled alpha discard and exposed the black RGB hidden in transparent texels as dark wireframes or cards in LODs.
- Unified render-layer leaf detection across the vanilla leaf class and the `minecraft:leaves` tag, so tagged modded leaves using custom block classes also follow the correct cutout path.

## 0.4.7（1.21.1 NeoForge 联动版）— 2026-09-10

### 简体中文

- 新增 FramedBlocks 远景兼容：将方块实体驱动的主/副伪装、强化状态、可折叠偏移及特殊模型数据保存为 Voxy 持久化变体，并在 LOD 烘焙时恢复完整模型数据与伪装染色。
- 区段缺少方块实体（例如服务端流式远景数据）时会复用同一 Voxy 世界缓存中的 FramedBlocks 变体，避免远处结构在重新摄取后退回空框架或错误材质。
- 联动配置新增“远景框架方块”开关和独立距离限制；机械动力及其航空学、交错电网附属联动按顺序排列并统一命名。
- PowerGrid 远景电线从仅覆盖悬垂线扩展为覆盖悬垂线、方块折线、软线和灯串软线；方块折线路径使用新版持久化格式精确保留，并兼容读取旧缓存。
- LittleTiles 远景网格新增独立半透明阶段，保留小方块颜色 Alpha、发光强度及逐朝向图集坐标，使透明材质和光影法线/高光采样不再被压成不透明单色。

### English

- Added distant FramedBlocks compatibility. Block-entity-driven primary/secondary camos, reinforcement, collapsible offsets, and special model data are stored as persistent Voxy variants and restored for LOD baking and camo tinting.
- FramedBlocks variants are reused from the same Voxy world cache when a streamed section has no block entities, preventing distant structures from reverting to empty frames or incorrect materials after re-ingestion.
- Added an independent FramedBlocks LOD toggle and distance limit, and reordered/renamed Create and its Aeronautics and PowerGrid integration groups.
- Expanded PowerGrid distant wires from hanging wires only to hanging, block-routed, cord, and string-light entities. Routed polylines use a backward-compatible persistent format.
- Added a dedicated translucent pass for LittleTiles LOD meshes and retained per-tile alpha, emission, and per-face atlas coordinates for shader normal/specular sampling.

## 0.3.3（26.1.2 NeoForge 客户端版）— 2026-09-07

### 简体中文

- 修复移除远离视野的层级节点时截断子节点指针，偶发以 `-1` 访问分配位图并崩溃的问题。
- 26.1.2 的构建与运行兼容基线调整为 Sodium 0.9.1，并将加载版本精确限制为 `0.9.1+mc26.1.2`。

### English

- Fixed a crash where removing distant hierarchy nodes truncated a child pointer and could access the allocation bitmap at index `-1`.
- Changed the 26.1.2 build and runtime compatibility baseline to Sodium 0.9.1 and restricted loading to exactly `0.9.1+mc26.1.2`.

## 0.3.3（1.20.1 Forge 客户端版）/ 0.3.2（26.1.2 NeoForge 客户端版）— 2026-09-06

### 简体中文

- 同步联动版的水与岩浆 LOD 修复：按流体状态保留液面高度，并以原版式邻接加权重建四角斜坡。
- 顶面、不同高度连接面与外围梯形侧面共用同一组角点，修复流柱层间细缝、特定方向侧面抬升及区段边界空洞。
- 粗 LOD 不再将单个孤立流体样本扩张为完整体素，透明纹理空像素使用稳定的流体颜色回退。
- 1.20.1 Forge 发布产物名称现在明确包含 `mc1.20.1`。

### English

- Backported the integrations-edition water and lava LOD fixes, preserving state-specific surface heights and reconstructing vanilla-style weighted four-corner slopes.
- Caps, height transitions, and perimeter trapezoid walls now share the same corners, fixing thin column seams, direction-dependent raised walls, and section-boundary holes.
- Coarse LODs no longer expand one isolated fluid sample into a full voxel, and transparent texture samples use a stable fluid-colour fallback.
- The 1.20.1 Forge release artifact now explicitly includes `mc1.20.1` in its filename.

## 0.4.5-beta.2（1.21.1 NeoForge 联动版）— 2026-09-06

### 简体中文

- 新增 Simulated 激光指示器远景光束：保留动态方向、命中长度、颜色、红石强度与末端渐弱，并在原版方块实体渲染距离外继续显示。
- 新增独立的 LOD 半透明绘制阶段，使激光正确使用 Voxy 地形深度，并接入 Iris 光影的透明 G-buffer；交界处保留安全重叠以避免闪烁。
- 新增远景激光开关与距离选项；仅安装 Simulated 时注册采集和渲染逻辑。PowerGrid 兼容状态更新为完全兼容。
- 将远景激光选项归入“航空学”，新增“小方块”和“模拟殖民地”远景开关与距离选项，并补充“交错电网”中文分组名称。
- 修复远景激光高于 LOD 地形、以天空为背景时被合成阶段裁掉的问题；光束现在写入 LOD 深度，可完整显示到末端。
- 修复水与岩浆在粗 LOD 中表面缩进被随层级放大，导致顶部缺面、侧面高出或形成深色竖墙的问题；顶面和侧面上缘现在共享固定世界尺度的流体高度。降采样也不再把单个孤立流体样本扩张为整个粗体素。
- 为水与岩浆的各个流动液位状态编码独立的 LOD 高度；LOD 0 保留原版液位轮廓，粗层级按状态恢复液面，并在相邻不同液位之间补齐连接侧壁，同时以微小顶面重叠消除网格缝和下层方块外露。
- 流动液体的内部高度差侧壁与最外围侧壁现在均严格截止到对应液位；保留原版斜顶面的同时，修复液位之间的空洞和外围侧面过高。
- 液位过渡面现在携带较低一侧的高度，只绘制两侧液面之间的差值区间，消除内部整墙重叠和残余缺面；同时修复草径、耕地等非满方块错误遮挡邻接完整侧面而产生的缝隙。
- 统一流体顶面与侧面的高度基准，不再混用邻域烘焙斜面和状态液位；取消对下落状态的无条件满高处理，修复仍然存在的内部横向空洞和外围细长高墙。
- 水平流体边界改为逐邻接生成，不再依赖会错位合并的 X/Z 流体扫描器；同高流体、液位台阶、空气边界和实体遮挡分别处理，以优先保证流体拓扑完整。
- 修复 X 向流体侧面把 Z 宽度误作 Y 高度的问题；X/Z 两个方向现在分别按各自面内坐标裁剪，消除满高细柱、窄片和对应缺口。
- 修复竖直流柱中同种流体因液位状态不同而生成内部水平面的错误；上方存在同种流体时按满高连接，不再在每个方块层之间留下细缝。
- 水与岩浆顶面改为按原版规则对邻接液位加权计算四角高度，并由 GPU 重建连续斜坡；顶面与外围梯形侧面共享角点，同种流体之间不再生成多余的高度差侧墙。
- 统一正负 X/Z 朝向及 32 方块区段边界的流体角点顺序；区段外侧面也改用四角梯形，并读取相邻区段液位，修复特定观察方向下侧面抬升和边界小缝。

### English

- Added distant Simulated laser-pointer beams, preserving dynamic direction, hit length, colour, redstone strength, and end falloff beyond block-entity render distance.
- Added a dedicated LOD translucent stage so lasers use Voxy terrain depth and the Iris translucent G-buffer, with a small safe handoff overlap to prevent flicker.
- Added independent distant-laser and distance options. Capture and rendering register only when Simulated is installed. PowerGrid is now marked fully compatible.
- Moved distant-laser options into Aeronautics, added Little Tiles and Domum Ornamentum enable/distance options, and localized the PowerGrid group name in Chinese.
- Fixed distant lasers being discarded against the sky above LOD terrain. Beams now write LOD depth and remain visible through their full length.
- Fixed water and lava surface indentation scaling with coarse LOD levels, which caused missing tops, over-tall sides, and dark vertical walls. Top faces and side rims now share a world-scale fluid height, and isolated single fluid samples no longer expand into a full coarse voxel.
- Encoded a dedicated LOD height for every flowing water and lava level. LOD 0 keeps the native profile, while coarser levels reconstruct state-specific surfaces, add connecting walls between adjacent levels, and use a minute cap overlap to eliminate grid seams and exposed blocks below.
- Internal step walls and outer walls of flowing fluids now end at their exact state height at every LOD level, preserving native sloped caps while fixing holes between levels and over-tall perimeter faces.
- Fluid transition faces now carry the lower neighbour height and draw only the interval between both surfaces, removing overlapping full walls and residual holes. Partial-height blocks such as dirt paths and farmland no longer incorrectly occlude a neighbour's complete side face.
- Unified fluid cap and wall height calculations instead of mixing neighbourhood-baked slopes with state heights. Falling states are no longer forced to full height unconditionally, fixing the remaining horizontal holes and thin over-tall perimeter walls.
- Horizontal fluid boundaries are now generated per adjacency instead of using the misaligned X/Z fluid scan mergers. Equal surfaces, level steps, air boundaries, and solid occluders are handled separately to preserve correct fluid topology.
- Fixed X-facing fluid walls treating their Z span as the Y-height coordinate. X and Z faces now use their respective in-plane axis order, removing full-height posts, narrow strips, and the matching holes.
- Fixed internal horizontal faces being generated between vertically stacked fluids of the same kind when their states differed. A matching fluid above now makes the column continuous and full-height between block layers.
- Water and lava caps now use vanilla-style weighted neighbouring heights at all four corners and reconstruct a continuous slope on the GPU. Caps and perimeter trapezoid walls share their corner heights, while adjacent fluids of the same kind no longer create redundant step walls.
- Unified fluid-corner ordering across positive and negative X/Z faces and 32-block section boundaries. Outer section walls now use the same four-corner trapezoids and neighbouring-section heights, fixing raised walls and small seams visible from specific directions.

## 0.4.4-beta.1（1.21.1 NeoForge 联动版）— 2026-09-06

### 简体中文

- 新增 PowerGrid 悬垂电线远景兼容：按原始端点、线长、粗细与颜色生成简化悬链线网格，并接入 Voxy 地形深度、光照和 Iris 光影管线。
- 电线快照随 Voxy 世界缓存持久化，在 PowerGrid 实体离开跟踪范围或重进世界后继续显示；靠近实体时交还原版渲染。
- 新增远景电线开关与距离选项；兼容逻辑仅在安装 PowerGrid 时注册。

### English

- Added distant rendering for PowerGrid hanging wires. Simplified catenary meshes preserve endpoints, cable length, thickness, and colour while using Voxy terrain depth, lighting, and the Iris shader pipeline.
- Wire snapshots persist with the Voxy world cache and remain visible after PowerGrid entities leave tracking range or the world is rejoined; native rendering takes over again nearby.
- Added independent distant-wire and distance options. The integration registers only when PowerGrid is installed.

## 0.3.2（1.20.1 Forge 客户端版）— 2026-09-05

### 简体中文

- 修复长时间浏览大量 LOD 后，几何缓冲区碎片化触发 `Geometry OOM` 并导致游戏崩溃的问题。
- 几何上传现会预检连续空间，在高占用时提前清理不可见节点；分配暂时失败时保留当前网格，待空间回收后安全重试。

### English

- Fixed a `Geometry OOM` crash caused by geometry-buffer fragmentation after viewing a large amount of LOD terrain.
- Geometry uploads now preflight contiguous space and reclaim invisible nodes earlier under pressure. A temporarily blocked allocation keeps the current mesh and retries safely after reclamation.

## 0.4.3-beta.2（1.21.1 NeoForge 联动版）— 2026-09-05

### 简体中文

- 修复已加载 LOD 最远边界把未知邻区当作空气、为水体生成向上黑色立面的问题；真实水岸与已加载邻区的水体侧面保持正常。
- 修复不同维度及高 Y 坐标下各级 LOD 地形逐级抬升，并保持表层方块材质与高度一致。
- 统一逐级降采样时的天空光与方块光传递，修复相邻 LOD 环明暗交替。

### English

- Fixed upward dark water walls at the far edge of loaded LOD data by distinguishing unavailable neighbours from real air; real shorelines and loaded-neighbour fluid faces remain intact.
- Fixed progressive terrain uplift across LOD levels in other dimensions and at high Y coordinates while preserving the correct surface material and height.
- Stabilized sky-light and block-light propagation during successive downsampling, fixing alternating brightness between adjacent LOD rings.

## 0.3.1（26.1.2 NeoForge 客户端版）— 2026-08-25

### 简体中文

- 修复新版 Iris 在 Mixin 选择阶段被过早加载导致的启动崩溃。
- 按 26.1.2 的最新源码更新至 Sodium 0.9.2-alpha.4，并同步 Iris 26.1 适配。
- 服务器数据共享现与 Voxy Server Side 完全兼容，推荐在服务器端部署。
- 部分代码使用 AI 辅助完成，发布前由维护者审核并进行必要的小幅修改，以提高开发效率。

### English

- Fixed startup crashes caused by the latest Iris being loaded too early during Mixin selection.
- Updated 26.1.2 compatibility to Sodium 0.9.2-alpha.4 and synchronized the Iris 26.1 integration.
- Server data sharing is fully compatible with Voxy Server Side, which is recommended on servers.
- Some code was completed with AI assistance and will be audited and lightly edited by the maintainer before release to improve development efficiency.

## 0.4.3-beta.1（1.21.1 NeoForge 联动版）— 2026-08-23

### 简体中文

- Domum-Ornamentum 兼容由初步支持提升为完全兼容：特殊组合方块使用精细独立 LOD 模型，正确保留材质、方向、拼接几何、光照与持久缓存。
- 修复远景信标光柱靠近原版视距边界时短暂消失：只有在原版信标实体和有效光柱均已就绪后才交接，并保留安全重叠距离。
- 信标列缓存缺失时改为后台低优先级预热；重算期间保留上一份完整光柱，避免存储读取阻塞渲染线程或产生闪烁。
- 信标宽度改为交界处平缓、极远处持续增长且无硬上限的连续曲线，降低刚超出原版距离时的突兀增粗。
- 远景信标光柱接入 Iris 阴影图阶段；仅对已缓存光柱提交轻量棱柱阴影几何。

### English

- Promoted Domum-Ornamentum from preliminary to full compatibility. Composite blocks now use detailed dedicated LOD models with correct materials, orientation, joined geometry, lighting, and persistence.
- Fixed distant beacon beams disappearing while approaching the vanilla boundary. Handoff now waits for a loaded beacon block entity with a valid vanilla beam and keeps a conservative overlap.
- Cold beacon columns now warm asynchronously at low priority. The last complete beam remains visible during a rebuild, avoiding render-thread storage reads and transient blinking.
- Replaced the beacon width step with a smooth, unbounded curve that grows gently near the handoff and remains visible at extreme distance.
- Added distant beacon shadow casters to the Iris shadow-map pass using lightweight cached prism geometry.

## 0.4.2-beta.1（1.21.1 NeoForge 联动版）— 2026-08-22

### 简体中文

- 新增 LittleTiles 静态小方块远景兼容：区块摄取时一次性量化为 1/8 格稀疏表面网格，保留方块纹理、LittleTiles 颜色、群系染色与光照，并剔除内部相邻面。
- 小方块网格随 Voxy 世界缓存持久化，每 tick 最多上传两个变化区段；LittleTiles 未安装时不注册相关渲染器。
- 修复重进世界时尚未就绪的 LittleTiles 方块实体误删持久化网格，以及原版视距与 LOD 交界处的小方块不可见带；交接改为带安全重叠的圆形距离判定。
- 修复 LittleTiles 远景仅存在于内存、执行 `/voxy reload` 或原地重进后消失的问题；快照更新会可靠写入 Voxy 辅助存储，并在重载、退出与同世界存储切换前执行检查点。
- 修复 LittleTiles 远景被固定满光照导致夜间或阴影中过亮；现使用快照保存的天空光、方块光和原版动态光照贴图。小方块跳过透明 LOD 淡入，并在淡入起点进行三维硬交接。
- 版本更新至 0.4.2-beta.1；四个版本的模组元数据与配置界面名称统一为 `Neo-Voxy`，并同步更新中英文兼容性表格。

### English

- Added distant rendering for static LittleTiles microblocks. Geometry is captured once during section ingestion, quantized into sparse 1/8-block surface meshes, and keeps block textures, LittleTiles colours, biome tinting, lighting, and internal-face culling.
- Microblock meshes persist with the Voxy world cache and upload at most two changed sections per tick. Without LittleTiles, no related renderer is registered.
- Fixed persisted LittleTiles meshes being deleted when block entities were not ready during world entry, and removed the invisible handoff band with a conservative circular vanilla/LOD overlap.
- Fixed LittleTiles distant meshes existing only in memory and disappearing after `/voxy reload` or rejoining in place. Snapshot updates are now persisted defensively and checkpointed before reload, logout, and same-world storage replacement.
- Fixed LittleTiles distant meshes being forced to full brightness. They now use captured sky/block light with the live vanilla light map, skip translucent LOD fading, and perform a hard 3-D handoff at the fade start.
- Updated the integrations edition to 0.4.2-beta.1, standardized the mod metadata and configuration-screen name across all four editions as `Neo-Voxy`, and refreshed both compatibility tables.

## 0.3.1（1.20.1 Forge / 26.1.2 NeoForge 客户端版）— 2026-08-20

### 简体中文

- 将 1.21.1 联动版的水面基准校正同步至 1.20.1 与 26.1.2，避免粗粒度 LOD 像素把连续水面切成高低不一的接缝。
- 将 26.1.2 的扩展区块请求从渲染设置中拆分为独立配置页，与其他客户端版本保持一致。
- 修复 1.20.1 与 26.1.2 原版及模组树叶在 LOD 中染色掩码、叶片识别或模型去重失效导致的浅色、透明感和零散黑块；群系染色叶片也统一走稳定 tint 路径。
- 修复藤蔓及其他被错误声明为 SOLID 的透明植物/薄片方块在 LOD 中变成黑色矩形的问题，烘焙阶段会按实际图集像素自动启用 alpha discard。
- 补齐 26.1.2 的雾气配置选项，并修复 1.20.1 无光影时 LOD 距离雾距离过远而几乎不可见的问题。
- 将 26.1.2 的叶片识别与无光影染色修复补齐到 1.20.1；修复未注册原版颜色提供器的樱花树叶被错误染成黑色的问题。
- 彻底移除 1.20.1 的 LOD 边界淡入路径，因为其造成了严重问题，并修复 Sodium 地形矩阵捕获时机导致的远景拖影填充天空问题。

### English

- Ported the integrations edition's water-datum correction to 1.20.1 and 26.1.2 so coarse LOD pixels no longer split continuous water surfaces into visible height seams.
- Moved extended chunk requests out of the 26.1.2 rendering settings and into a separate configuration page, matching the other client editions.
- Fixed vanilla and modded 1.20.1 and 26.1.2 leaves appearing washed out, transparent, or speckled with black blocks in LODs when tint masks, leaf identification, or model deduplication failed; biome-tinted leaves now use the same stable tint path.
- Fixed vines and other transparent plant/thin-quad blocks incorrectly declared as SOLID from becoming black rectangular cards in LODs; baking now enables alpha discard from the actual atlas sprite pixels.
- Restored the full fog settings group on 26.1.2 and fixed 1.20.1 no-shader LOD fog using a distance so far away that it was effectively invisible.
- Completed the 26.1.2 leaf-identification and shader-free tint fixes on 1.20.1; unregistered vanilla colour providers no longer turn cherry leaves black in LODs.
- Removed the 1.20.1 LOD boundary fade path entirely and fixed sky-filling terrain trails caused by capturing Sodium terrain matrices at the wrong time.

## 0.4.1beta（1.21.1 NeoForge 联动版）/ 0.3.0（客户端版）— 2026-08-18

### 简体中文

- 将 GPU 世界曲率统一移植至 1.21.1、1.20.1 与 26.1.2；曲率从原版渲染边界外开始，避免边界台阶和按区段跳动。
- 客户端版统一版本号为 0.3.0，联动版更新为 0.4.1beta，并修复 26.1.2 包内版本元数据。
- 统一三档树叶 LOD 模式及配置入口；保留隐藏内部面剔除。
- 向旧版降级移植水体优先的 LOD 降采样、占用率就近取整和光照选择，减少流体破碎与 LOD 系统性抬升。
- 进服消息改为每次进入世界显示，默认开启，并可在 Sodium/Embeddium 的 Neo Voxy 设置中关闭；消息会直接提示关闭位置。
- 将联动版最新圆形 LOD 淡入同步至三个客户端版：使用三维相机距离与世界坐标稳定抖动，水体独立交接，树叶跳过透明淡入，减少空洞、噪点及树叶消失后重现。
- 将扩展区块请求同步至全部客户端版，默认关闭并限制为 48 区块；移动时暂停扩张，静止后渐进恢复，避免集中请求拖垮帧时间。
- 将 LOD 群系水色混合同步至全部客户端版；在模型构建时写入紧凑调色板。
- 修复 1.20.1 与 1.21.1 无光影远景雾距离，补全 1.20.1 水下雾遮罩，并使失明、黑暗正确遮挡 LOD；26.1.2 保留新版原生雾路径。
- 同步逐面独立 mip、精确逐像素染色掩码、水体边界分类及相关降采样修复，改善植被、水体和原版/LOD 交界质量。
- 远景信标的完整缓存渲染继续由联动版提供，避免在缺少数据索引的客户端版引入无效选项。

### English

- Ported GPU world curvature across 1.21.1, 1.20.1, and 26.1.2. Curvature starts outside the vanilla boundary to avoid a seam and section-relative snapping.
- Unified client-edition versions at 0.3.0, updated the integrations edition to 0.4.1beta, and fixed embedded 26.1.2 version metadata.
- Unified the three leaf LOD modes and their settings while retaining hidden internal-face culling.
- Down-ported water-aware LOD sampling, nearest occupancy rounding, and light selection to reduce broken fluids and systematic LOD uplift.
- Join messages now appear on every world entry, are enabled by default, and can be disabled in the Neo Voxy Sodium/Embeddium settings; the message states where to disable it.
- Synchronized the integrations edition's latest circular LOD handoff to all three client editions: 3D camera distance, world-stable dithering, independent water ownership, and direct leaf replacement reduce holes, noise, and leaf disappear/reappear transitions.
- Extended chunk requests are now available in every client edition, disabled by default and capped at 48 chunks. Expansion pauses while moving and resumes gradually while stationary to protect frame time.
- Added build-time LOD biome water-colour blending to every client edition using a compact palette.
- Fixed shader-free distant fog on 1.20.1 and 1.21.1, restored the 1.20.1 underwater fog mask, and made Blindness/Darkness hide LODs correctly. Version 26.1.2 retains its newer native fog path.
- Synchronized per-face mip generation, exact per-pixel tint masks, fluid boundary classification, and related downsampling fixes for better vegetation, water, and vanilla/LOD boundary quality.
- Full cache-backed distant beacons remain in the integrations edition instead of exposing a non-functional client-only toggle.

## 0.4.0-beta-2 (1.21.1 NeoForge 联动版) — 2026-08-17

### 简体中文

- 实验性 LOD 轻量光照启用失败时在聊天框提示具体回退原因。
- 增加 NeoVoxy 内置的 Eclipse Shader 482 Lite 补丁，无需修改光影包；保留其 GBuffer 契约并简化远景透明反射与材质输入。
- Lite 着色器在驱动编译失败时同步更新状态、在聊天框提示并回退。
- 合并 LOD 群系水体颜色混合，提供 0–7 混合半径和可选草木混色；默认仅处理水体。
- 方块图集读取改用原生缓冲区，规避大型图集同步读取期间固定 Java 数组导致的 JVM 崩溃风险。
- 模型烘焙线程补充中断退出、初始化资源回收与请求锁异常恢复。
- 构建阶段检查中英文语言键和 JSON 完整性。

### English

- Lite LOD shading now reports its fallback reason in chat when it cannot be enabled.
- Added a built-in NeoVoxy Lite patch for Eclipse Shader 482, requiring no shader-pack changes while preserving its GBuffer contract and simplifying distant translucent reflections and material inputs.
- Lite driver-compilation failures now update the visible status, notify chat, and fall back.
- Added LOD biome water colour blending with a 0–7 radius and optional grass/foliage scope; water-only by default.
- Block-atlas readback now uses native memory to avoid pinning a large Java array across a synchronous GPU read.
- Hardened model-bakery shutdown, constructor cleanup, and request-lock recovery.
- Added build-time JSON and translation-key validation.

## 0.4.0-beta-1 (1.21.1 NeoForge 联动版) — 2026-08-17

### 简体中文

- 加入实验性 LOD 专用轻量光影管线；光影包可提供成对的 Lite 不透明/透明程序，在远景减少逐材质光照开销。
- Lite 默认关闭，在 Iris 构建或重载光影管线时检查。
- 加入三层保护：设置页显示实际启用/回退状态；Lite 程序必须声明一致的光影包、测试版本与契约版本；未满足距离过渡条件时整套回退标准程序。
- 提供经声明验证的 Complementary Unbound r5.8.1 + Euphoria Patches 1.9.3 实验补丁模板；水体保留完整材质和反射路径。

### English

- Added an experimental shader-pack-supplied Lite LOD pipeline that can replace the opaque and translucent LOD programs as an atomic pair.
- Lite shading is off by default and is resolved while Iris builds or reloads a pipeline.
- Added three safeguards: visible active/fallback status, matching pack/version/API declarations, and an enforced safe distance-transition contract with full-pair fallback.
- Added a declared test implementation for Complementary Unbound r5.8.1 + Euphoria Patches 1.9.3; water retains its full material and reflection path.

## 0.3.3-1.20.1-alpha.1 — 2026-08-11

### 简体中文

- 完成 Forge 1.20.1 原生客户端移植的发布前稳定性修复，不依赖 Connector 或 Fabric API。
- 修复无光影时雾气选项不生效；区分水下等必要环境雾与可选的远景雾，并按 LOD 距离计算天空雾。
- 修复樱花等透明树叶在无光影时显示为黑块。
- 补全配置界面的中英文文本及 SSAO 模式名称。
- 加入地面植物交叉 LOD 模型、性能/平衡/质量三档树叶模式，以及按帧预算工作的 LOD 构建压力控制。
- 保留此前完成的原版/LOD 边界、水体侧面、Oculus 管线重载和光影历史拖影修复。
- 1.20.1 暂不包含圆形 LOD 淡入及任何模组联动；Oculus 光影支持仍为实验性功能。

### English

- Completed the release-preparation stability pass for the native Forge 1.20.1 client port, with no Connector or Fabric API dependency.
- Fixed shader-free fog settings; required medium fog such as underwater fog is separated from optional distant fog, and sky fog now uses the LOD distance.
- Fixed transparent leaves such as cherry leaves rendering as opaque black without shaders.
- Completed Chinese and English configuration text, including localized SSAO mode names.
- Added crossed ground-plant LOD models, Fast/Balanced/Quality leaf modes, and frame-budgeted LOD build-pressure control.
- Retained the existing vanilla/LOD boundary, water-side, Oculus pipeline reload, and shader-history ghosting fixes.
- The 1.20.1 edition currently excludes circular LOD handoff and all mod integrations; Oculus shader support remains experimental.

## Multiversion — 2026-08-10

### 简体中文

- 将 1.21.1 NeoForge 联动版、1.21.1 NeoForge 纯客户端版、1.20.1 Forge 纯客户端版和 26.1.2 NeoForge 纯客户端版整合到统一多版本仓库。
- 提供四版本独立构建与一键全版本构建；发布产物会剔除无用原生库、重复模块描述符和构建中间文件。
- GitHub Actions 展示四个独立构建任务，最终只发布一个 `neo-voxy-multiversion` 整合产物。

### English

- Combined the 1.21.1 NeoForge integrations edition, 1.21.1 NeoForge client edition, 1.20.1 Forge client edition, and 26.1.2 NeoForge client edition in one multiversion repository.
- Added per-edition and all-edition build commands; release artifacts remove unused natives, duplicate module descriptors, and build intermediates.
- GitHub Actions exposes four individual build jobs and publishes only the final `neo-voxy-multiversion` bundle.
